export default class PlayoffsPair {
    constructor(id, games, prevIds, nextId, teams) {
        this.id = id
        this.games = games
        this.prevIds = prevIds
        this.nextId = nextId
        this.teams = teams
    }
} 